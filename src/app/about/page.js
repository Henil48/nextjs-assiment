
export default function AboutPage() {
  return (
    <section style={{ padding: '2rem' }}>
      <h1>About Us</h1>
      <p>
        This is the About page. We are building a full stack Next.js app with routing,
        navbar, and theme toggle — all with plain CSS.
      </p>
    </section>
  );
}
